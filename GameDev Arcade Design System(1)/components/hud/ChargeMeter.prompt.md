Bow draw-power meter — fades in while the player holds the draw, fills cyan→white.

```jsx
<ChargeMeter value={charge} show={drawing} />
```
`value` 0–1. Place bottom-center, just under the crosshair.
