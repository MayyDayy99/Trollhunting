First-person aiming reticle whose ticks fly apart as the bow draws — drive `spread` from the charge value and `element` from the equipped arrow.

```jsx
<Crosshair spread={charge} element="fire" />
```
Center dot + 4 ticks; ticks tint to the element (white for base).
