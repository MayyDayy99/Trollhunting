Small, wide-tracked (.42em) uppercase kicker that sits above a title — sets the section or mode.

```jsx
<Eyebrow tone="gold">Mohás Berci · játék</Eyebrow>
```

Tones: `faint` (default blue-grey), `gold`, `p1` (cyan), `p2` (orange). Keep it to 1–3 short words.
