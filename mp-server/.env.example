# LAN IP of the host, injected into the container so the server can hand out
# shareable invite links that work from OTHER devices (localhost does not).
# Copy this file to `.env` and fill in your machine's LAN IPv4, then
# (re)start the server. Re-detect and update it if your network/IP changes.
HOST_IP=192.168.x.x
